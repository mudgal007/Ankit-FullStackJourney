1) debounce(fn, delay):  A function that delays the execution of a function for a 
given time. It is useful for preventing a function from being executed multiple times unnecessarily.

2.throttle(fn, delay): A function that limits the execution of a function to a 
given time. It is useful for preventing a function from being executed multiple times unnecessarily.

3.createCancellableFetch(fetchImpl, { cacheTtlMs = 5000 }): A function that creates a 
cancellable fetch function. It is useful for creating a cancellable fetch function that can be used to fetch data from a url.

4.keyOf(url, opts): A function that creates a key for a given url and opts. It is useful for creating a key for a given url and opts.

5.cancellableFetch(url, opts): A function that fetches data from a url. It is useful for fetching data from a url.

6.cancellableFetch(url, opts): A function that fetches data from a url. It is useful for fetching data from a url.